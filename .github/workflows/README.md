# Workflows

- `dsg-openscad.yml` regenerates the OpenSCAD design-document images for the tube clamp and uploads them as a workflow artifact.

A later workflow can decide whether validated design images should be committed automatically or updated through a dedicated documentation-update process.
