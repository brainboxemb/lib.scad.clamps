workspace:
  inheritedTasks:
    include:
      - scad.docs
      - scad.verify

tasks:
  scad.docs:
    inputs:
      - 'openscad/**/*.scad'
      - 'openscad/**/design/**'
      - 'pythonscad/**/*.py'
      - 'pythonscad/**/design/**'
      - 'scripts/**'

  scad.verify:
    inputs:
      - 'doc/30-verification.md'
      - 'openscad/**/*.scad'
      - 'pythonscad/**/*.py'
      - 'test/**'
      - 'vrf/**'
      - 'scripts/**'
      - 'bootstrap.sh'
      - 'bootstrap.ps1'
      - 'update-repo.sh'
      - 'update-repo.ps1'
      - '.github/workflows/**'
      - 'tools/tool.git-project'
